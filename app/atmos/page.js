import { ProductDetailPage } from "@/components/product-detail-page";

export const metadata = {
  title: "Sonicite Atmos | 不只是被听见，而是被感知",
  description: "Sonicite Atmos 是面向品牌与空间的活的声音身份系统。",
};

export default function AtmosPage() {
  return <ProductDetailPage product="atmos" />;
}
